
<div class="card">
    
    <div>
        <h2><%= message.username %></h2>
        <p><%= message.add %></p>
        <p><%= message.text %></p>
        <% if (message.username === locals.user.username)  {%>
            <div>
                <form action="/home/<%= message.id %>/delete" method="POST">
                    <button>Delete</button>
                </form>
                <form action="/home/<%= message.id %>/edit" method="POST">
                    <button>Edit</button>
                </form>
            </div>
        <% } %>
    </div>
</div>